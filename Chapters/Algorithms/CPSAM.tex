\subsection*{Cellpose-SAM}
Combines the Cellpose architecture with the foundational model SAM.
Achieves, according to their evaluation methods, superhuman generalization, breaking the interannotator bound.
additionally to the SAM based model, Mouseland also provides one based on DINO
The cellpose model uses a U-Net type NN , predicting vector flows, which equals the r a representation of the segmentation. categorie of CNN. using the gradient of heach vector a mask is produced a t pixel level. 
SAM: model scale , dataset size, and total training compute improve response to 'propt prompt engineering' -> promptabl with masking at realtime lvl. 
SAM-Model: image encoder -> image embedding + prompt encoder embeds prompts --> combine to lightweight mask decoder predicting segm asks 
thus the impage embedding can be used separately from prompt encoder , i.e. different prompter can be used. see CPSAM
keeping the major part of the SAM (305M out of 312M parameters) they combine the ViT encoder from SAM with the decoding of Cellpose(?) . predicting the vector output immediatly without anything inbetween 
the reworked some basics of the encoder: from highres image to smaller inputs with small pathces (1024->256,16->8) ; local attention in SAM back to default global attention as in ViT-L (does this not kinda work against the SAM approach? issn't the point the local attention?) "with almost no runtime penalty" | is that relevatn? | why did they do that?
their focus apparently lies mostly on the SAM-Weights! since the training approach by SAM is unsupervised it can train on millions of input images with little to nto human labor. big data good !
they train the pretained model on 22.826 images with \~ 3M ROIs. while continuing with Ceellpose3 mixing probabilites for down weighting homogenous datasets (what is this what does this maen?)
