\subsection*{Proseg}
cell simulation as segmentation
probability based
using improved cellular pots models
assigns transcripts by simulated gene expression
MCMC
stemming from the underlying method of probabilistic segmentation

CPM: simulates cell behaviour based on objective function
	e.g.: two cell types should not touch, if they do they energy increases
MCMC-inference: simulate cells to best explain observed transcripts
purely statistical modeling, meaning that every decision can be traced back to the probabilities | which are based on a "physical" model
-------------------------
probability based alorithms have been in  a string decline with the rise of ML models. publishing a new model, based on vbery old POTTs models seemed very bold and it turned out to be one of the most robust alorithms. it more deterministic than the other modles. no feature extraction or similar necessary. best case, a new loss function is provided and the alorithm continues to outperform MLs